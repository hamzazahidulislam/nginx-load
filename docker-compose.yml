# @format

version: '3'
services:
  nginx_load_balancer:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - '80:80'
app1:
  image: node-app
  environment:
    - APPID=First-App
app2:
  image: node-app
  environment:
    - APPID=Second-App
app3:
  image: node-app
  environment:
    - APPID=Third-App
